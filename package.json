{
  "name": "whimsync",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "bun --cwd apps/api dev",
    "dev:worker": "bun --cwd apps/worker dev",
    "dev:web": "bun --cwd apps/web dev",
    "dev:extractor": "uv run --directory apps/extractor python -u -m src.main",
    "test": "bun --env-file=.env.test test",
    "test:api": "bun --cwd apps/api test",
    "test:worker": "bun --cwd apps/worker test",
    "test:extractor": "uv run --directory apps/extractor pytest",
    "db:generate": "bun --cwd packages/db db:generate",
    "db:migrate": "bun --env-file=.env packages/db/src/migrate.ts",
    "db:push": "bun --cwd packages/db db:push",
    "db:studio": "bun --cwd packages/db db:studio",
    "lint": "biome check .",
    "lint:extractor": "uv run --directory apps/extractor ruff check .",
    "format": "biome check --write .",
    "format:extractor": "uv run --directory apps/extractor ruff format .",
    "typecheck": "bun --workspaces run typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.3"
  }
}
