{
  "name": "worker",
  "scripts": {
    "dev": "bun --env-file=../../.env run --hot src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@whimsync/db": "workspace:*",
    "bullmq": "^5.41.0",
    "ioredis": "^5.5.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
